namespace FallbackPlan.Repository.Index;

/// <summary>
/// Marks this assembly for reflection-based tooling — architecture tests and
/// fixture discovery. Exists so tests can name an assembly without depending on
/// an arbitrary production type that may later move or be renamed.
/// </summary>
public static class AssemblyMarker;
